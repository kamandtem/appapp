# حذف پنج ژست اصلی بدون عکس

- پنج رکورد اول `ALL_BUILTIN_POSES` از `INITIAL_POSES` حذف شدند.
- هیچ فایل عکس `pose-*.webp` در بسته وجود نداشت؛ بنابراین asset اضافه‌ای حذف نشد.
- ۱۰۰ ژست imported قبلی و ۵۷ ژست batch3 بدون تغییر باقی ماندند.
- مسیر فیلتر، storage، transfer و نمایش از `INITIAL_POSES` تغذیه می‌شود و با ۱۵۷ رکورد باقی‌مانده معتبر است.
