# مرحله ۱: ترفندهای ژست‌دهی

این نسخه بخش مستقل «ترفندهای ژست‌دهی» را به منوی آتلیتو اضافه می‌کند.

امکانات تکمیل‌شده:
- نمایش آفلاین ۱۲۰ ژست و تصویر اصلی آن‌ها
- دسته‌بندی همه، خانم، دونفره، تک‌نفره و گروهی
- جستجو در نام، توضیحات، تگ‌ها و مراحل
- صفحه جزئیات هر ژست با مراحل اجرا، تنظیمات دوربین، خطاهای رایج و تگ‌ها
- حالت بارگذاری، خطا و نتیجه خالی
- طراحی واکنش‌گرا برای موبایل و نمایش بزرگ‌تر
- داده‌ها جدا از کتابخانه اصلی آتلیتو نگه داشته شده‌اند تا ترجمه مرحله بعد بدون دست‌کاری معماری فعلی انجام شود

وضعیت زبان:
- رابط کاربری فارسی است.
- محتوای ۱۲۰ ژست در فایل فارسی مرحله دوم اضافه شده است. این داده‌ها فقط در بخش «ترفندهای ژست‌دهی» مصرف می‌شوند و به کتابخانه اصلی ژست‌های آتلیتو اضافه نشده‌اند.

بررسی:
- اتصال منو و ناوبری بررسی شد.
- تطابق ۱۲۰ رکورد با ۱۲۰ تصویر بررسی شد.
- در محیط فعلی Node.js/npm نصب نبود، بنابراین build اجرایی اینجا ساخته نشد. پروژه برای اجرای `npm run typecheck` و `npm run build` آماده است.
