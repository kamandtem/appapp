+# وضعیت پرداخت درون‌برنامه‌ای بازار
+
+## تنظیمات ثابت برنامه
+
+- شناسه بسته اندروید: `com.atelito.app`
+- شناسه محصول: `premium_access`
+- نوع محصول در پنل بازار: محصول یک‌بارخرید و غیرمصرفی
+- محصول نباید Consume شود، چون خرید باید پس از نصب مجدد قابل بازیابی باشد.
+
+## کارهای انجام‌شده در کد
+
+- اتصال به بازار با Poolakey و کلید عمومی RSA انجام می‌شود.
+- خرید فقط با تطبیق شناسه محصول، نام بسته، وضعیت خرید، توکن و امضای خرید فعال می‌شود.
+- خریدهای قبلی هنگام اجرای برنامه و با دکمه «بازیابی خرید قبلی» بررسی می‌شوند.
+- اگر بازار با موفقیت پاسخ بدهد ولی خریدی وجود نداشته باشد، دسترسی پریمیوم حذف می‌شود.
+- اگر بازار موقتاً در دسترس نباشد، دسترسی قبلاً تأییدشده حذف نمی‌شود.
+- محدودیت‌های نسخه رایگان برای ژست‌ها، پروژه‌ها، آفیش‌ها، همکاران و اعضای آتلیه فعال‌اند.
+- ساخت Android در GitHub Actions انجام می‌شود، بنابراین نبود پوشه `android` در فایل منبع عمدی است.
+
+## کارهایی که باید در پنل بازار انجام شوند
+
+1. برنامه با شناسه `com.atelito.app` در پنل توسعه‌دهندگان بازار ثبت یا انتخاب شود.
+2. در بخش پرداخت درون‌برنامه‌ای محصول غیرمصرفی با شناسه دقیق `premium_access` ساخته شود.
+3. کلید عمومی RSA پنل با مقدار داخل `src/services/bazaarBilling.ts` تطبیق داده شود.
+4. برای تست، قیمت محصول موقتاً روی مبلغ آزمایشی مورد قبول بازار تنظیم شود.
+5. یک نسخه امضاشده با Version Code جدید در پنل بارگذاری شود؛ پرداخت روی نسخه‌ای که در پنل بارگذاری نشده قابل تست کامل نیست.
+
+## سناریوهای تست اجباری روی گوشی
+
+1. کاربر بدون بازار یا بدون ورود به حساب، پیام خطای روشن ببیند و پریمیوم نشود.
+2. لغو صفحه پرداخت باعث فعال‌شدن پریمیوم نشود.
+3. خرید موفق بلافاصله همه محدودیت‌ها را بردارد.
+4. بستن و بازکردن برنامه خرید را حفظ و دوباره با بازار بررسی کند.
+5. حذف و نصب مجدد برنامه، با همان حساب بازار، خرید غیرمصرفی را بازیابی کند.
+6. دکمه بازیابی برای حساب بدون خرید، پریمیوم را فعال نکند.
+7. قطع اینترنت پس از یک خرید تأییدشده، دسترسی قبلی را بی‌دلیل حذف نکند.
+
+## نکته امنیتی
+
+اعتبارسنجی فعلی برای یک برنامه آفلاین مناسب است و پاسخ امضاشده Poolakey را بررسی می‌کند.
+
+برای جلوگیری جدی از دست‌کاری روی دستگاه‌های روت‌شده، مرحله بعدی یک API سمت سرور برای بررسی Purchase Token با API توسعه‌دهندگان بازار است.
