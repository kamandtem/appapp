# رفع قطعی اسکرول افقی فیلتر مرحله‌ها

- علت مشکل این بود که ریل چیپ‌ها خودش `width: max-content` داشت و از viewport بیرون می‌زد؛ در نتیجه اسکرول روی عنصر اشتباه اعمال می‌شد.
- ساختار اصلاح شد: `scenario-stage-scroll` به‌عنوان viewport با `width: 100%` و `overflow-x: auto` اضافه شد و `scenario-stage-options` فقط ردیف محتوای کامل چیپ‌هاست.
- همه ۱۴ مرحله باقی می‌مانند و با لمس/کشیدن افقی قابل مشاهده‌اند، مشابه فیلتر حالت بدن.
