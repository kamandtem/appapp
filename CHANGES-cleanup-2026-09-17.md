# پاک‌سازی و اصلاح کلی ژست‌ها

- ژست‌های فعال: 157 (۱۰۰ imported قبلی + ۵۷ batch3) به‌علاوه ۵ ژست اصلی.
- عکس‌های canonical حذف‌شده: 197 مورد (`pose-006.webp` تا `pose-202.webp`).
- عکس‌های حذف‌شده دیگر در هیچ رکورد فعال ارجاع ندارند.
- حالت ژست، حرکت، سناریو و زیردسته باغ عمارت با عنوان و تصویر بازتنظیم شد.
- تگ‌های فیلتر برای دسته، حالت، سناریو و کادر روی همه رکوردها تکمیل شد.
- سیستم حذف runtime همچنان از `deletedBuiltinIds` استفاده می‌کند و عکس رکوردهای حذف‌شده در سورس باقی نمی‌ماند.
