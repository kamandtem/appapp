# رفع نمایش ناقص ژست‌ها

- مشکل اصلی پیدا شد: در حالت گروه‌بندی کتابخانه، رابط کاربری از هر سناریو فقط `slice(0, 4)` را نمایش می‌داد؛ بنابراین تعداد محدودی از ژست‌ها دیده می‌شدند، نه کل رکوردهای فعال.
- نمایش به `g.poses.map(card)` اصلاح شد تا همه ژست‌های هر سناریو در کتابخانه دیده شوند.
- نسخه مبنا همان بسته اصلاح‌شده ۱۵۷ ژستی است، بدون محدودیت نسخه رایگان.
- همه فایل‌های `pose-*.webp` از `public/generated/photos` حذف شدند؛ کارت‌های پنج ژست اصلی در نبود عکس، fallback داخلی عنوان ژست را نمایش می‌دهند و خراب نمی‌شوند.
- ۱۵۷ رکورد imported و batch3 حفظ شدند.
